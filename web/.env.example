# Public URL of the FastAPI service. Used for REST calls and to build
# the WebSocket URL (substituting https→wss / http→ws automatically).
# Example: NEXT_PUBLIC_API_URL=https://codeflow-production-27c6.up.railway.app
NEXT_PUBLIC_API_URL=
